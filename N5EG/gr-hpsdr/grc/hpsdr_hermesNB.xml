<?xml version="1.0"?>
<block>
  <name>hermesNB</name>
  <key>hpsdr_hermesNB</key>
  <category>hpsdr</category>
  <import>import hpsdr</import>
  <make>hpsdr.hermesNB($RxF, $RxSmp, $RxPre)</make>
  <callback>set_ReceiveFrequency($RxF)</callback>
  <callback>set_RxSampRate($RxSmp)</callback>
  <callback>set_RxPreamp($RxPre)</callback>
 <!-- Make one 'param' node for every Parameter you want settable from the GUI.
       Sub-nodes:
       * name
       * key (makes the value accessible as $keyname, e.g. in the make node)
       * type -->
  <param>
    <name>Receive Frequency, Hz.</name>
    <key>RxF</key>
    <value>7200000</value>
    <type>real</type>
  </param>
  <param>
    <name>Rx Sample Rate</name>
    <key>RxSmp</key>
    <value>192000</value>
    <type>int</type>
  </param>
  <param>
    <name>Rx Preamp On/Off</name>
    <key>RxPre</key>
    <value>0</value>
    <type>int</type>
  </param>

  <!-- Make one 'sink' node per input. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <sink>
    <name>in</name>
    <type>complex</type>
  </sink>

  <!-- Make one 'source' node per output. Sub-nodes:
       * name (an identifier for the GUI)
       * type e.g.   int, real, complex, byte, short, xxx_vector, ...
       * vlen
       * optional (set to 1 for optional inputs) -->
  <source>
    <name>out</name>
    <type>complex</type>
  </source>
  <doc>
  This block is the HPSDR Hermes/Metis module.
  It supports one receiver in the Narrow Band (digital downconversion) mode.
  Receive Frequency, Receive Sample Rate, and Receive Preamp can be set.
  Currently does not support transmit operation, nor more than one receiver.
  </doc>
</block>
